/**
 * Kept so `node src/server.js` still works for anyone with it in muscle memory
 * or an old script. The real entry point is app.js at the repository root,
 * which is what package.json `main` and every managed host resolve to.
 */
import '../app.js';
